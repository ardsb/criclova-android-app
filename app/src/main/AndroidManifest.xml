<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.criclowa">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CricLowa">
        <activity android:name=".View.AddBattingStatisticActivity"></activity>
        <activity android:name=".View.DisplayPlayersDetailsActivity" />
        <activity android:name=".View.DisplayPlayersProfileDetailActivity" />
        <activity android:name=".View.AddPlayerDetailActivity" />
        <activity android:name=".View.AddMatchActivity" />
        <activity android:name=".View.PlayerStatisticActivity" />
        <activity android:name=".View.SplashActivity" />
        <activity android:name=".View.RegisterActivity" />
        <activity android:name=".View.HomepageActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".View.LoginActivity"
            android:windowSoftInputMode="adjustResize" />
    </application>

</manifest>